# This is a Spack Environment file.
#
# It describes a set of packages to be installed, along with
# configuration settings.
spack:
  # add package specs to the `specs` list
  specs: []
  view: false
  concretizer:
    unify: true
  config:
    install_tree:
      root: $env/opt/spack
  repos:
    excalibur: /home/ucapkei/Documents/uksrc-excalibur-tests/benchmarks/spack/repo
